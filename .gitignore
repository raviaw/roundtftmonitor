# build artifacts (regenerable)
firmware/**/build/

# python bytecode cache
__pycache__/
*.pyc

# OrcaSlicer CLI drops this in the cwd (headless has no OpenGL, so it skips
# thumbnail rendering and says so). Nothing to keep.
00000.log

# os cruft
Thumbs.db
.DS_Store

# unrelated items present in this folder
docker/
start-docker-container.cmd

